<!DOCTYPE html>
<html lang="es">
    <head>
        <link rel="icon" href="./images/favicon.ico">
        <title>Hospital Dr. Alejandro Korn - Administración</title>
        <link rel="stylesheet" href="./css/style.css">
        <link rel="stylesheet" href="./css/mobileStyle.css">
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
    </head>
    <body>
        <header class="box">
            <div class="leftCenter">
                <a href="./index.php?categoria=administracion">
                    <img src="./images/logo.svg" title="Cruz Roja, logo del Hospital." alt="Cruz Roja, logo del Hospital.">
                </a>
                <nav>
                    <ul>
                        <li><a href="./index.php?categoria=administracion" >Inicio</a></li>
                        <li><a href="">Consultas</a></li>
                        <li><a href="">Pacientes</a></li>
                        <li class="dropdown">
                            <a href="javascript:void(0)" class="dropbtn">Administracion</a>
                            <div class="dropdown-content">
                                <a href="./index.php?categoria=usuarios">Usuarios</a>
                                <a href="#">Roles</a>
                                <a href="#">Permisos</a>
                                <a href="#">Configuracions</a>
                            </div>
                        </li>
                    </ul>
                </nav>
            </div>
            <div class="rightCenter" >
                <a>{{user.username}}</a>
                <a href='./index.php?accion=logout'>Cerrar Sesion</a>
            </div>
        </header>
            <main class="lightBox">
                <h3>Mostrar Usuario {{ usuario.username }}</h3>
                <table>
                    <tr><th>Nombre de usuario: </th><td>{{ usuario.username }}</td></tr>
                    <tr><th>Nombre: </th><td>{{ usuario.firstName }}</td></tr>
                    <tr><th>Apellido: </th><td>{{ usuario.lastName }}</td></tr>
                    <tr><th>E-mail: </th><td>{{ usuario.email }}</td></tr>
                    <tr><th>Estado de cuenta: </th><td>{% if usuario.activo == 1 %} Activo {% else %} Inactivo {% endif %}</td></tr>
                    <tr><th>ID del usuario: </th><td>{{ usuario.id }}</td></tr>
                    <tr>
                        <th>Roles: </th>
                        {% for rol in roles %}
                            <td>=> {{ rol }}</td>
                        {% endfor %}
                    </tr>
                    <tr><th>Fecha y hora de creacion:</th><td>{{ usuario.createdat|date("d/m/Y H:i") }}</td></tr>
                    <tr><th>Fecha y hora de actualizacion:</th><td>{{ usuario.updatedat|date("d/m/Y H:i") }}</td></tr>
                </table>
            </main>
            <footer class="box">
                <span class="leftCenter">Proyecto de Software 2018 - Hospital Dr. Alejandro Korn</span>
                <span class="rightCenter">v 0.1.0</span>
            </footer>
    </body>
</html>